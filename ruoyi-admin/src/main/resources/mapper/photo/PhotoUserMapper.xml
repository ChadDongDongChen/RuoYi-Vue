<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.web.photo.mapper.PhotoUserMapper">

    <resultMap type="PhotoUser" id="PhotoUserResult">
        <result property="id"            column="id"              />
        <result property="openid"        column="openid"          />
        <result property="unionId"       column="union_id"        />
        <result property="nickname"      column="nickname"        />
        <result property="avatarUrl"     column="avatar_url"      />
        <result property="status"        column="status"          />
        <result property="lastLoginTime" column="last_login_time" />
        <result property="createTime"    column="create_time"     />
        <result property="updateTime"    column="update_time"     />
    </resultMap>

    <sql id="selectUserVo">
        select id, openid, union_id, nickname, avatar_url, status, last_login_time, create_time, update_time
        from photo_user
    </sql>

    <select id="selectByOpenid" parameterType="String" resultMap="PhotoUserResult">
        <include refid="selectUserVo"/>
        where openid = #{openid}
    </select>

    <select id="selectById" parameterType="Long" resultMap="PhotoUserResult">
        <include refid="selectUserVo"/>
        where id = #{id}
    </select>

    <insert id="insertUser" parameterType="PhotoUser" useGeneratedKeys="true" keyProperty="id">
        insert into photo_user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="openid != null and openid != ''">openid,</if>
            <if test="unionId != null">union_id,</if>
            <if test="nickname != null">nickname,</if>
            <if test="avatarUrl != null">avatar_url,</if>
            <if test="status != null">status,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="openid != null and openid != ''">#{openid},</if>
            <if test="unionId != null">#{unionId},</if>
            <if test="nickname != null">#{nickname},</if>
            <if test="avatarUrl != null">#{avatarUrl},</if>
            <if test="status != null">#{status},</if>
        </trim>
    </insert>

    <update id="updateUser" parameterType="PhotoUser">
        update photo_user
        <trim prefix="SET" suffixOverrides=",">
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="status != null">status = #{status},</if>
        </trim>
        where id = #{id}
    </update>

    <update id="updateLastLoginTime">
        update photo_user set last_login_time = now() where id = #{id}
    </update>
</mapper>
